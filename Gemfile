source "https://rubygems.org"

gemspec

# to be on gemspec when it's solid gem
gem 'stones', :git => 'git@github.com:epdp/stones.git', :branch => 'config' #:tag => '1.0.0-pre5'

group :development, :test do
  gem 'minitest'
  gem 'pry'
  gem 'pry-plus'
end
